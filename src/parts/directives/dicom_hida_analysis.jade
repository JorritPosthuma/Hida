h1 Starting HIDA analysis
h2 First, we need more information about the patient:

span
  input(ng-model="ctrl.weigth", placeholder="Please fill in the weigth...")
  span.postfix kg
span
  input(ng-model="ctrl.length", placeholder="And the length...")
  span.postfix cm

i.submit.fa.fa-connectdevelop(ng-click="ctrl.analyse()", ng-class="{valid: ctrl.isValid()}")